source 'https://rubygems.org'

# Use Sinatra framework
gem 'sinatra'

# Server index file from Redis
gem 'redis'

# Rack a Ruby webserver interface
gem 'rack'

# Allow to use Puma as webserver
gem 'puma'
